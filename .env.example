# Example environment configuration for the e‑commerce API

# Connection string for your PostgreSQL instance. This format assumes a local
# database server; update the username, password, host and database name to
# match your environment. When using Docker Compose the host is the name of the
# `db` service.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ecommerce?schema=public"

# Secret used to sign JSON Web Tokens. Change this value in production.
JWT_SECRET="change_this_secret"

# Port where the server will listen. Defaults to 4000 if not set.
PORT=4000